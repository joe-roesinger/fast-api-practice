[project]
name = "my_project"
version = "0.1.0"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]

server = [
    "fastapi[standard]>=0.100.0",
]

[tool.mypy]
pytohn_version = "3.10"
warn_return_any = true
warn_unused_configs = true
